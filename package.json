{
  "name": "safetycrm",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "docker:dev": "cd docker && docker compose -f docker-compose.dev.yml up -d",
    "dev": "npm run docker:dev && turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.9",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}